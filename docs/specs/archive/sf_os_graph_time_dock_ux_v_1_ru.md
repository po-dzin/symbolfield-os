# SF HUD – Graph + Time Dock UX

## 0. Онтология

**ArcheCore** – нулевая точка / seed аватара. Вся цифровая микориза вырастает из этого узла.

Дальше уровни:

- **Domain cores** – крупные жизненные домены / миры (SYMBOLFIELD, CENTER WAY, BODY, RELATIONS и т.п.).
- **Project cores** – отдельные арки/продукты/кластеры практик.
- **Ritual / State / Insight nodes** – листья и мхи на ветках (ежедневка, состояния, инсайты).

Граф = живой организм, растущий из archecore во все стороны.

---

## 1. Режимы: NOW-линза, GRAPH-режим, HUD

### 1.1 NOW-линза

**Назначение:** фокус на одной (или паре) нод.

- Работает **только для активной ноды** (или до 2–3 нод в мультиоконном режиме).
- Открывается на **фулл-скрин**, вокруг – минимальное окружение из ближайших 1–2 хопов связей.
- Показывает:
  - базовые свойства ноды (название, тип, SSA5-слой, XP, домен/проект);
  - историю действий (логи, изменения, SEM7-стадии);
  - привязанные ритуалы / стейты / теги;
  - мини-спираль времени **этой ноды** (жизненный цикл).
- Чувство: “живое досье на сущность” + контекст ближайших связей.

**Вход:** дабл-клик по ноде в графе.

**Выход:** закрытие линзы (ESC / back / жест) → возврат в тот же участок графа.

### 1.2 GRAPH-режим (Graph-tab)

**Назначение:** единое пространство для обзора **и** модификации микоризы.

- Это и есть основной **граф-таб**.
- Минимальный уровень масштаба = **DAY**. Даже при минимальном зуме видно дневной срез.
- Метрика отображения:
  - **позиция** – ветви вокруг archecore (radial layout);
  - **глубина** – расстояние по графу от archecore;
  - **время** – влияет на яркость/движение/размер, а не жёсткую координату.
- Цветовая система:
  - **SSA5 слой** → базовый цвет мха (🟥🟨🟩🟦🟪);
  - **XP тип (HP/EP/MP/SP/Q/NP)** → акцент/сияние/частицы;
- NOW-фокус в GRAPH-режиме:
  - ноды с высокой актуальностью (recency + frequency) слегка вытягиваются к камере, крупнее и ярче;
  - старые и пассивные – уходят в глубину, блекнут.
- Базовые операции редактирования прямо в этом режиме:
  - добавление/удаление нод и связей;
  - перемещение/пинning ключевых нод;
  - быстрые группировки и автолейауты.

### 1.3 HUD-таб (DataViz) (DataViz)

**Назначение:** фильтрация и аналитика поверх графа.

- Не редактирует структуру графа, только **меняет отображение**.
- Фильтры:
  - по количеству связей (degree);
  - по времени создания / последней модификации;
  - по тегам / доменам / проектам;
  - по SSA5 слою / XP типу;
  - по кастомным группам и saved views.
- Режимы маппинга:
  - раскраска по SSA5;
  - кластеризация по доменам / проектам;
  - heatmap активности.

---

## 2. Time Dock + Time Spiral = линза времени

Time Dock и кольцевая спираль времени работают как **единый контроллер Twindow** – масштаба временного среза.

### 2.1 Twindow уровни

Уровни Twindow:

- **NOW** – последние 1–2 часа активности.
- **DAY** – текущие сутки.
- **WEEK** – 7–10 дней.
- **MOON** – \~28–35 дней.
- **SEASON** – \~3 месяца.
- **YEAR** – 12 месяцев.

Twindow управляет:

- `Tmax` – максимальный возраст видимых нод;
- `Dmax` – максимальная глубина по графу от acrchecore.

### 2.2 Поведение в Graph-view

При смене Twindow:

- **NOW**:
  - показываются только ноды в окне последних часов + их ближайшие предки;
  - граф максимально разреженный, как HUD задач.
- **DAY**:
  - добавляются все события дня, ветви утолщаются;
- **WEEK / MOON**:
  - проступают устойчивые паттерны и корни проектов;
- **SEASON / YEAR**:
  - видна почти вся микориза, NOW-узлы ещё сильнее вытянуты к пользователю.

### 2.3 Интерфейс Time Dock

- Верхняя строка: текущий Twindow (`NOW / DAY / WEEK / MOON / SEASON / YEAR`) – тап/клик для прыжка.
- Центральная часть: мини-спираль с отметками важных событий (thresholds, релизы, Q/NP). Клик по отметке → камера фокусируется на соответствующем под-графе, Twindow подстраивается.
- Нижняя часть: статусы и счётчики (количество активных нод, доминирующие домены, баланс XP за Twindow).

### 2.4 Связка с другими панелями

- **State panel** (mode/tone/glyph) – влияет на визуальный стиль NOW-узлов.
- **XP panel** – считает метрики по тому же Twindow.
- **Graph-view** – фильтрует ноды по `Tmax` и `Dmax`.

Все три читают один и тот же `Twindow`.

---

## 3. Взаимодействия с нодами

### 3.1 Базовые клики

- **Single-click** по ноде в графе:

  - активирует ноду;
  - открывает **окно свойств** (панель), где видно:
    - название, тип, домен/проект;
    - SSA5-слой, XP-тип;
    - ключевые теги;
    - быстрые настройки (закрепить, выделить, добавить тег, включить в группу/представление).

- **Double-click** по ноде в графе:

  - погружение в **NOW-линзу** этой ноды на фулл-скрин;
  - вокруг – ближайшие 1–2 хопа связей;
  - слева/справа – timeline/спираль жизни ноды;
  - есть кнопка возврата “Back to Graph”.

### 3.2 Мультиоконный режим

- Возможность активировать **до 2–3 нод одновременно**:
  - split-screen NOW-линзы;
  - сравнение / сшивка паттернов между двумя сущностями.
- Ограничение по количеству окон → не дробим экран и не теряем фокус.

### 3.3 Навигация

- Scroll / pinch на графе → изменение Twindow (масштаб времени + глубина).
- Drag на пустом пространстве → перемещение камеры.
- Hover по ноде → тултип с краткой инфой.

---

## 4. Цвет и материал (SSA5 + XP)

### 4.1 SSA5 → базовый цвет

- 🟥 Sensorial Substrate – тёплые красно-землистые.
- 🟨 Expressive Flow – жёлто-янтарные, текучие.
- 🟩 Symbolic Abstraction – зелёно-теаловые, сеточные.
- 🟦 Symbolic Awareness – синие/циановые, дымка/небеса.
- 🟪 Meta-Void Integration – фиолетовые, почти UV.

### 4.2 XP → акцент / эмиссия

- HP – тяжёлые формы, низкие пульсации.
- EP – частицы, капли, небольшие струи.
- MP – острые блики, быстрый noise.
- SP – мягкое дыхание свечения.
- Q/NP – спектральные сдвиги, глитч-рефракции.

Так один и тот же граф можно читать как **слоистую карту сознания**, не теряя строгой структуры.

---

## 5. Тех-реализм (веб vs десктоп)

### 5.1 Веб-реализация

- Рендер: **three.js / react-three-fiber** (WebGL/WebGPU) – всё на GPU пользователя.
- Бэкенд: лёгкий Node/TS + graph DB (Neo4j / Postgres + расширения).
- Облако: бюджетные/фри тарифы (Railway/Render/Supabase и аналоги).
- Оптимизации:
  - instanced meshes для “мха”;
  - LOD и агрегация дальних кластеров;
  - ограничение количества одновременно видимых нод в NOW/Day.

### 5.2 Когда нужен десктоп/игровой движок

- Если понадобятся:
  - тяжёлая физика, VR/AR, full-perf лайв-сеты;
  - глубокая интеграция с железом (миди, сенсоры тела, многомониторные сетапы);
  - полноценный офлайн-режим и локальное хранение всего графа.

Тогда следующий шаг:

- десктопный клиент (Tauri/Electron как мост к веб-ядру) или сразу Unity/Godot-движок.

---

## 6. Дорожная карта (вкратце)

1. **v0 – Web Graph Explorer**

   - 3D граф вокруг meta-core;
   - базовая цветовая схема SSA5/XP;
   - Twindow: DAY/WEEK переключатель;
   - single/double-click по ноде (свойства + простая NOW-панель).

2. **v1 – Полноценный HUD + Time Dock**

   - кольцевая time-spiral;
   - все уровни Twindow (NOW→YEAR);
   - HUD-фильтры (degree, time, tags, SSA5, XP);
   - NOW-линза как отдельный фулл-скрин режим.

3. **v2 – Аудио/виз скульптура и мультиокна**

   - аудио-движок, привязанный к графу;
   - мультиоконный режим NOW;
   - сохранение “сцен” как артефактов (картинка + саунд + мета).

Этот канвас = актуальная спецификация для UX/архитектуры Graph + Time Dock + NOW-линзы в SF HUD.

