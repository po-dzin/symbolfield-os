# UI_ICON_PICKER_SoT_v0.5_r1
**Status:** Source of Truth (SoT)  
**Scope:** SF v0.5 — Icon Picker (Visual Key / KeySlot) для Node/Hub/Core

---

## 0) TL;DR
Icon Picker вводит **единый слот Visual Key (keySlot)** для визуальной метки ноды:  
`glyph | emoji | icon | upload` (один слой, без каскада).

Цель v0.5:
- убрать **буквы-аватары**,
- дать ясные дефолты (◉/◎/empty),
- обеспечить быстрый выбор + recents/favorites,
- подготовить задел под auto-by-meaning и Gen* (позже, за флагом).

---

## 1) Термины

- **Visual Key / KeySlot** — **единый слот** визуальной метки ноды, который отображается **в графе** (и в header). Значение слота может быть: `glyph | emoji | icon | upload`.
- **glyph** — короткий unicode-знак/сигил (1–3 графемы), вводимый вручную (например: `∴`, `⛩️`, `◉`).
- **emoji** — unicode emoji из набора (технически тоже unicode, но с отдельным индексом/поиском).
- **icon** — svg-иконка из библиотеки.
- **upload** — загруженное изображение (PNG/JPG/WEBP; SVG опционально и только при sanitize).
- **Core node** — кор-нода спейса.
- **Hub node** — контейнер/группировка.
- **Regular node** — обычная нода.

---

---

## 2) Дефолтные правила (вместо букв)

### 2.1 Default Icon Policy

| Тип ноды    | Дефолтная иконка | Когда показывать                  | Когда скрывать                               |
| ----------- | ---------------- | --------------------------------- | -------------------------------------------- |
| **Core**    | **◉**            | всегда, если нет кастом-иконки    | никогда                                      |
| **Hub**     | **◎**            | всегда, если нет кастом-иконки    | никогда                                      |
| **Regular** | *(пусто)*        | не показывать ничего по умолчанию | всегда, пока user не выбрал icon/emoji/glyph |

### 2.2 Замена букв

- Раньше: если нет иконки → показывались инициалы/буква.
- Теперь: **инициалы запрещены** (feature flag: `avatar_letters=false`).
- Если нет иконки: применяются правила из таблицы выше.

### 2.3 Приоритет (один слой)

Есть **один слот** `keySlot`.
- Если `keySlot` задан (glyph/emoji/icon/upload) → он отображается в графе.
- Если `keySlot` пустой → применяется `defaultPolicy` (◉/◎/empty).

---

---

## 3) Точка входа (entry points)
- Click по зоне иконки в заголовке ноды (NodeHeader).
- Node Context Menu → **Set icon…**
- Hotkey: `I` (если фокус/selection на ноде) → открыть пикер.

---

## 4) IA и UX
## 3) Icon Picker — UX/IA

### 3.1 Точка входа

- Клик по области иконки в заголовке ноды.
- Контекстное меню → **Set icon…**
- Горячая клавиша: `I` (если фокус на ноде) → открыть пикер.

### 3.2 Общий каркас

- Tabs (v0.5 MVP): **Glyph | Emoji | Icons | Upload**
- Tabs (v0.7–0.8): + **Gen\*** (скрыто за feature flag до поздних версий)
- Search (единый): placeholder `Search icon…`.
- Справа в хедере: **Remove** (сброс к дефолту).
- Внизу: **Cancel / Save** (или `Apply`), preview выбранного.

### 3.3 Поведение сохранения

- **Single click**: выбрать (preview), но не применить.
- **Double click**: применить сразу (power-user).
- `Enter` = Save, `Esc` = Cancel.

### 3.4 Recents + Favorites

- В каждой вкладке сверху ряд: **Recent** (последние 12) + ⭐ Favorites.
- Recents общие по всем типам (emoji/icon/upload/gen) и по scope пользователя.

### 3.5 Поиск + ранжирование

Ранжирование результатов:

1. exact match по label/tags
2. synonym/alias match
3. contextual boost (по типу ноды и месту: Docs/Tasks/etc)
4. favorites/recents boost

---

## 4) Вкладки

### 4.0 Glyph (ручной)
- Быстрый ввод **1–3 графем** (unicode) в слот.
- Валидация (MVP):
  - буквы (латиница/кириллица) по умолчанию **не используем** как визуальный ключ: `allow_letters_in_glyph=false`.
  - max length: 3 grapheme clusters.
- UI: поле ввода + **Recent glyphs**.

### 4.1 Emoji
- Источник: unicode emoji index.
- Поиск по keywords + aliases (пример: `doc` → 📄📑🗂️🧾).
- UI: сетка, hover tooltip.

### 4.2 Icons (library)
- Источник: локальная библиотека SVG.
- Стиль:
  - 24×24, `stroke`-based, `currentColor`.
  - 1 вес на MVP (например 2px).
- Категории (MVP): Document / Structure / Graph / Media / Action.

### 4.3 Upload
- Formats: PNG/JPG/WEBP (+SVG опционально при sanitize).
- Input: Upload button + paste (`⌘/Ctrl+V`) + paste URL.
- Processing:
  - center-crop square
  - generate thumbs: 256/64
  - max: 5MB, max dim: 2048

---

## 4.x Gen* (v0.7–0.8, НЕ MVP)
- Генерация глифов **отложена** до v0.7–0.8.
- В v0.5: вкладка скрыта/отключена feature flag `gen_glyph_enabled=false`.

---

## 5) Автоподбор «по смыслу» (future agent, v0.5 fallback без генерации)

### 5.1 Что значит «по смыслу»
Входные сигналы:
- `nodeType`: core/hub/regular
- `title` (строка)
- `tags[]` (если есть)
- `components[]` (например: Doc/Task/Ritual/Media)
- `spaceContext` (опционально)

Выход:
- предложить **6 вариантов** ключа (glyph/emoji/icon/upload*) + confidence.

### 5.2 Fallback-политика (v0.5 без агента)
- Если пользователь нажал **Auto** (кнопка в пикере) и нет агента:
  - core → ◉
  - hub → ◎
  - regular → пусто
  - + небольшой набор ручных маппингов (emoji/icon) по ключевым словам.

### 5.3 Мини-словарь маппинга (v0.5, без LLM)
| Смысл (keywords) | Emoji fallback | Icon fallback |
| --- | --- | --- |
| doc, note, spec | 📄 | page-text |
| task, todo | ✅ | check |
| hub, cluster | 🕸️ | nodes |
| media, image | 🖼️ | image |
| ritual, breath | 🫁 | wave |

### 5.4 API контракт (для будущего агента)

`POST /icon/suggest`

```json
{
  "nodeId":"...",
  "nodeType":"core|hub|regular",
  "title":"IREM Docs",
  "tags":["docs","spec"],
  "components":["Document"],
  "styleHint":"minimal|mycelium|crystal"
}
```

Response:

```json
{
  "suggestions":[
    {"kind":"emoji","id":"emoji:U+1F4C4","label":"Document","score":0.76},
    {"kind":"icon","id":"icon:page-text","label":"Page","score":0.71},
    {"kind":"glyph","text":"∴","label":"Sigil","score":0.63}
  ]
}
```

---

## 6) Данные (IconAsset)

### 6.1 Типы

- `glyph` — короткий unicode-текст (1–3 графемы)
- `emoji` — unicode emoji
- `icon` — svg из библиотеки
- `upload` — url + thumbs

### 6.2 Схема

```json
{
  "id":"icon:page-text",
  "kind":"glyph|emoji|icon|upload",
  "label":"Page",
  "tags":["doc","file"],
  "source":{
    "unicode":"📄",
    "svg":"<svg...>",
    "url":"https://...",
    "text":"∴"
  },
  "favorite":false,
  "usageCount":12,
  "lastUsedAt":"2026-01-01T00:00:00Z",
  "ownerScope":"personal|workspace|global"
}
```

---

---

## 5) Tabs (v0.5)
### 5.1 Glyph
- Ввод 1–3 графем (unicode).
- Валидация:
  - `allow_letters_in_glyph=false` (латиница/кириллица не используем как key по умолчанию)
  - max length: 3 grapheme clusters
- UI: input + Recent glyphs

### 5.2 Emoji
- Источник: unicode emoji index
- Поиск по keywords/aliases
- UI: grid + hover tooltip

### 5.3 Icons (library)
- Источник: локальная SVG-библиотека
- Стиль: 24×24, stroke-based, `currentColor`, единый вес
- UI: grid + поиск + категории

### 5.4 Upload
- Поддержка: SVG/PNG/WebP (MVP: ограничить список)
- Приведение: нормализация размеров
- Хранение: file storage + ссылка в keySlot

### 5.5 Gen* (не v0.5)
- Скрыто за флагом `gen_glyph_enabled=false`
- В v0.5 допускается только “fallback словарь” (без LLM) — если нужен auto-by-meaning.

---

## 6) Данные и контракты
### 6.1 Data model (минимум)
`node.key_slot` (nullable):
- `kind`: glyph|emoji|icon|upload
- `value`: string (unicode или icon id) / url (upload)
- `meta`: optional (pack, tags, createdAt)

### 6.2 Apply / Undo
Icon picker генерирует **Action**:
- `SetNodeKeySlot(nodeId, kind, value)`
- `ClearNodeKeySlot(nodeId)`

И пишет **EventLog**:
- `icon_selected`
- `icon_removed`

Undo/Redo работает по Actions (events остаются фактами).

---

## 7) Guardrails (не шумим обычному юзеру)
- Default: Regular nodes без иконок (пусто), пока юзер не выбрал.
- Core/Hub всегда имеют дефолт (◉/◎), если keySlot пустой.
- Не показываем буквы-аватары: `avatar_letters=false`.

---

## 8) Acceptance Criteria (v0.5)
- [ ] Click по NodeHeader icon открывает пикер.
- [ ] `I` открывает пикер для выбранной ноды.
- [ ] Выбор применяет keySlot и обновляет граф-рендер.
- [ ] Есть Recents (12) и Favorites.
- [ ] ESC закрывает без изменений; Enter сохраняет.
- [ ] Default policy (◉/◎/empty) работает и заменяет буквы.
