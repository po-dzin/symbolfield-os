# SymbolField HUD — Гибридная архитектура

## 1. Ядро: Символический граф-интерфейс

- Центральный объект: холст/граф узлов (символов, навыков, сущностей).
- Узел = глиф + краткий тайтл, связь = линия  (тип связи кодируется стилем/иконкой).
- Граф = одновременно карта, меню и журнал прогресса.
- Должен масштабироваться: фильтры, уровни зума, кластеры.

Опорные референсы:

- Rumor Mode (Outer Wilds) как знаниевый граф.
- Canvas/Graph View (Obsidian) как пользовательский Knowledge Map.

## 2. HUD: Контекстный, полу-дигетический

- Базовый режим: максимум экрана занимает холст/граф.
- HUD-элементы (XP/Mode/Skill Tree) появляются:
  - по действию (hover/click, переключение режима),
  - или привязаны к конкретному объекту (глиф над узлом, мини-овер).
- Постоянно видимы только 1–2 «жизненно важных» индикатора (например, общий XP-статус + текущий Mode).
- Раскладка и стиль должны позволять перенос в AR/VR (аналог «шлемного» HUD вокруг поля зрения).

Опорные референсы:

- Минималистичный HUD Destiny (только критичное, остальное по запросу).
- Дигетический HUD Outer Wilds (визор/панель как естественная плоскость интерфейса).

## 3. Кросс-модульный поток данных

- Единая модель данных (граф/БД), общая для веб, игровых движков и AR/VR.
- Любое событие = изменение графа:
  - новый узел, новая связь,
  - изменение свойств (XP, уровень навыка, статус Mode и т. д.).
- Фронтенды (web, game, AR) — просто разные виды на один и тот же граф.
- Двусторонняя связка:
  - действие в игре/AR сразу видно в веб-HUD/графе,
  - изменение в веб-интерфейсе влияет на поведение или визуал в игровых/AR-сценах.

Опорные референсы:

- Destiny: трекинг квеста в одном экране → трекер в HUD.
- Outer Wilds: находка в мире → новый узел в Ship Log.

## 4. Баланс минимализма и удобства

- Принцип The Witness: каждый элемент интерфейса должен «зачем-то быть».
- Но в отличие от игры, нужны мягкие опоры:
  - онбординг через простые «обучающие» узлы/глифы;
  - всплывающие подсказки по hover, а не постоянные текстовые подписи;
  - минималистичные визуалы прогресса (кольца, шкалы, маленькие диаграммы вместо громких RPG-баров).
- Ввести фокус-режимы:
  - режим глубокого погружения без HUD (аналог Flowstate),
  - режим обзора/дашборда (аналог SciNote) с компактной сводкой XP/Mode/Skill Tree.

## 5. Краткая формула гибридного стиля

- Структура и модульность: **Destiny**.
- Граф знаний и мягкое ведение: **Outer Wilds** + **Obsidian**.
- Радикальная чистота и символизм: **The Witness**.
- Практичный дашборд-прогресс: **SciNote**.

→ Цель: живой символический холст, где прогресс ощущается как загорающиеся узлы и связи, а HUD — как редкий, точный акцент, а не постоянный шум.

## 6. Визуальный стиль UI/UX (детализированная спека)

### 6.1. Базовые принципы

- ◇ **Мягкий, дышащий glassmorphism**
  - Размытые слои, лёгкие блики, мягкие градиенты.
  - Никаких кислотных бликов и «глянцевого казино»; ощущение матового, чуть туманного стекла.
- ○ **Zen-минимализм**
  - Мало элементов, максимум смысла.
  - Отсутствие жёстких рамок, лишних разделителей и текстового шума.
- ✧ **ОС как живой кристалл**
  - Впечатление, что ядро системы светится изнутри.
  - Свет/цвет и анимация исходят из центров (нод, ядра HUD), а не навешиваются сверху.
- ∴ **Глифоцентричность**
  - Основной язык интерфейса — символы и глифы; текст — подпись, а не главный носитель значения.

### 6.2. Цвет и стекло

- Фон: глубокий тёмный градиент (графит → тёмно-синий/фиолетовый).
- Акценты (кристаллическая палитра):
  - бирюзовый/циан — основное действие, активные элементы,
  - мягкий фиолетовый — режимы, мета-состояния,
  - тёплый янтарный — предупреждения, мягкие ошибки.
- Glassmorphism для карточек/окон:
  - `border-radius: 16–24px`
  - `backdrop-filter: blur(12–24px)`
  - `border: 1px solid rgba(255,255,255,0.06)`
  - фон панели: полупрозрачный, 5–15% opacity поверх размытого фона.

### 6.3. Типографика

- Современный гротеск (Inter / SF Pro / Manrope-класс).
- Три уровня иерархии на один экран (H, подзаголовок, body), не больше.
- Цвет текста:
  - основной: почти-белый (#F5F5F7),
  - вторичный: #9CA3AF–#6B7280,
  - подсказки/мета: #4B5563.

## 7. Глифоцентрированная система

### 7.1. Базовые глифы SF

- Закрепить • ∣ ○ ⊙ ∴ 𓂀 ∅ как базовый «алфавит состояния».
- Для каждого глифа определить:
  - форму и контур (толщина линий, радиусы),
  - цветовой диапазон,
  - характер анимации (пульс, свечение, вращение и т. п.).

### 7.2. Иконки и пиктограммы

- Единый грид (например, 24×24) для всех иконок.
- Толщина линий 1.25–1.5 px, скруглённые концы.
- Стиль — одноцветный outline + лёгкий glow в активном состоянии.
- Никаких перегруженных деталей; максимум 2–3 ключевые черты на иконку.

### 7.3. Текст vs символ

- Стандарт: **глиф + короткий label** (1–2 слова) для ключевых элементов.
- Длинные описания — только в раскрывающихся панелях, tooltip или detail-view узла.

## 8. Лэйаут и мультиоконность

### 8.1. Базовый layout (web)

- Центр: основной **канвас/граф SymbolField** (60–80% ширины, панорамный зум/пэн).
- Слева: узкий nav-rail с глифами модулей (HUD, Graph, Log, Agent, Settings).
- Справа/снизу: докуемые панели (Detail view, XP/Skill Tree, Log, Agent и т. д.).

### 8.2. Мультиоконный режим (Cursor / Obsidian-like)

- Любая панель — плавающее окно c состояниями: `floating | docked | minimized`.
- Окно можно:
  - перетаскивать по холсту,
  - прижимать к краям (snap to left/right/bottom),
  - сворачивать до иконки в док-баре.
- Рекомендуемый лимит: 3–4 активных окна одновременно.

### 8.3. Ноды и связи

- Ноды:
  - формы: круг (скилл), таблетка (модуль), прямоугольник (док/запись),
  - внутри: глиф слева, заголовок справа,
  - прогресс (XP) — через кольцевую/линейную подсветку контура.
- Связи:
  - тонкие линии, при hover — подсветка,
  - тип связи кодируется стилем (сплошная, пунктир, с маленьким глифом на середине).

## 9. Окна модулей и агент

### 9.1. Базовые типы окон

1. Graph / Канвас (центр).
2. Detail / Node View (правый док).
3. Agent Window (чат / командный интерфейс ИИ).
4. XP / Skill Tree (мини-дашборд прогресса).
5. Log / Timeline (история действий, сессий, изменений в графе).

Каждое окно:

- имеет заголовок с глифом модуля,
- минимальный header (название, pin, close, pop-out),
- кастомный тонкий скроллбар, ненавязчивый.

### 9.2. Окно агента

- Визуал: «кристаллический терминал» — стеклянная панель с чуть более плотным фоном.
- В углу — глиф агента (например, 𓂀 или спец-иконка).
- Может быть **привязан к узлу**: при открытии рядом с нодой агент сразу получает её контекст.

UX-паттерны:

- Клик по ноде → справа открывается Detail.
- Alt/Option+клик по ноде → справа открывается Agent с контекстом этой ноды.

## 10. Анимации и микро-взаимодействия

### 10.1. «Дыхание» интерфейса

- Idle-состояния:
  - лёгкий пульс прозрачности или glow у ключевых глифов (1–2% амплитуда, период 4–6 сек),
  - никаких резких вспышек.
- Hover:
  - малый scale-up (1.03–1.05),
  - мягкая подсветка контура / inner glow.

### 10.2. Переходы

- Открытие окна: fade + лёгкий slide (8–12 px).
- Перемещение по графу (пан/зум): плавное, с инерцией, без резких «телепортов».
- Смена Mode/XP-состояния: короткая анимация кольцевой подсветки вокруг соответствующего глифа (0.3–0.5 сек).

## 11. Стоп-линии и анти-паттерны

- Не более 3–4 разных визуальных приёмов на одном экране (цвет, blur, glow, градиент, тени — выбирать 2–3).
- Нет «классического веба» с синими ссылками и подчёркиваниями; взаимодействие через глифы и мягкие лейблы.
- Ошибки — без агрессивного красного: тёплый тон + ясный текст, что делать.
- Ни одной рамки или линии «просто так»: каждый разделитель должен реально улучшать читаемость.

Эта секция фиксирует визуально-UX основу SymbolField OS: мягкий стеклянный zen-интерфейс, живой кристалл-граф, глифы как основной язык и мультиоконная навигация без лишнего шума.

## 12. Режимы графа — техническая спека

### 12.1. Режим A: Плоский граф (Flat Node View)

**Назначение**  
Базовый рабочий режим для навигации по знаниям, скиллам и объектам. Оптимизирован под чтение, клики, связность.

**Модель данных**  
- Node { id, type, title, glyph, xp_state, state_tags, time_tags, position2D, cluster_id? }  
- Edge { id, source_id, target_id, relation_type, weight, is_directed }  
- Cluster { id, label, color, nodes[] } (опционально).

**Лэйаут**  
- Пространство графа — бесконечный 2D-холст, координаты в условных юнитах (например, `[-1000, 1000]` по осям).  
- Алгоритм раскладки (минимальный):
  - по умолчанию force-directed / spring layout,  
  - опционально — «кольца» вокруг центральных нод (radial layout) для отдельных корней.
- Масштабирование:
  - масштаб `zoom` линейный или экспоненциальный (колесо мыши / pinch),  
  - при изменении зума меняется уровень детализации:  
    - far: только точки/глифы кластеров,  
    - mid: ноды + краткие тайтлы,  
    - near: ноды + тайтлы + базовые метрики (XP/статус).

**Интеракции**  
- ЛКМ по ноде → открывает/фокусирует Detail-панель по этой ноде.  
- Alt/Option+ЛКМ по ноде → открывает Agent-панель с контекстом ноды.  
- Shift+drag → выбор нескольких нод (для массовых операций / фильтра).  
- Scroll / pinch → zoom, drag → pan.

**Производительность**  
- Цель: комфорт до ~300–500 видимых нод без заметных лагов.  
- При превышении порога включать авто-кластеризацию: несколько нод объединяются в aggregated cluster-node с указанием количества.

---

### 12.2. Режим B: Созвездия / био-облака (Constellation View, 2.5D)

**Назначение**  
Визуальное «органическое» представление графа как живого организма/созвездия: кластеры, глубина, объём. Используется для обзора, ритуалов, презентаций и глубокого созерцания.

**Модель данных (дополнительно к 2D):**  
- Node получает атрибут `depth` (слой по оси Z, например −1, 0, +1).  
- Cluster получает атрибуты `sphere_radius`, `center2D`, `depth`.  
- Edge может иметь стайл `orbit` (кривые/дуги) и параметр прозрачности.

**Рендеринг 2.5D**  
- Координаты нод такие же, как в плоском режиме, но добавляется глубина: 
  - глубина влияет на масштаб (чуть меньше/больше),  
  - и на прозрачность (дальние слои более прозрачные).  
- Кластеры могут визуализироваться как полупрозрачные сферы/облака, внутри которых разбросаны ноды.  
- Лёгкий параллакс: при движении камеры/панорамировании слои с разной глубиной смещаются с чуть разной скоростью.

**Интеракции**  
- Hover по кластеру → он «выныривает»: увеличивается контраст и яркость, остальные кластеры притухают.  
- Клик по кластеру → zoom внутрь (камеры), ноды кластера становятся основными.  
- Клик по ноде внутри → те же правила, что в плоском режиме (Detail/Agent).  
- Горячая клавиша/кнопка: переключение `Flat ⟷ Constellation` с анимированным переходом (позиции нод интерполируются).

**Уровни детализации (LOD)**  
- Дальний уровень: отображаются только сферы-кластеры, без отдельных нод.  
- Средний: основные ноды + яркие связи.  
- Ближний: все ноды, связи, дополнительные декоративные элементы (орбиты, микро-спайки).

**Ограничения**  
- Этот режим дороже по рендеру; по умолчанию включается для средних графов/отфильтрованных подмножеств.  
- Для больших графов > N нод использовать агрегацию и ограничение глубины.

---

## 13. Гармоническая система пропорций (ϕ, π, e)

**Цель:** задать единый математический «скелет» интерфейса, чтобы размеры холста, панелей, нод, глифов и анимаций подчинялись универсальным гармоническим отношениям. Это важно и для визуальной цельности, и для интерактивной «настройки» системы продвинутым пользователем.

### 13.1. Базовые константы

- ϕ (phi, золотое сечение) ≈ 1.618  
- π ≈ 3.14159  
- e ≈ 2.71828  
- Базовый модуль сетки: `U = 8 px` (единица шага для отступов, размеров и радиусов).

### 13.2. Горизонтальное деление экрана

Пусть `W` — полная ширина рабочего пространства.

- Центральный граф-канвас: `W_graph ≈ 0.618 · W` (≈ ϕ⁻¹).  
- Остальное пространство `W_side ≈ 0.382 · W` делится:  
  - левый nav-rail: `W_nav ≈ 0.146 · W` (≈ ϕ⁻³),  
  - правый док-пояс панелей: `W_dock ≈ 0.236 · W` (≈ ϕ⁻²).

Адаптивность: при малых экранах допускается диапазон `W_graph ∈ [0.55W; 0.75W]`, остальные области ужимаются пропорционально с сохранением приблизительных отношений ϕ.

### 13.3. Вертикальное деление

Пусть `H` — высота.

- Верхний технический пояс (header / минимальный статус-бар): `H_header ≈ 0.146 · H`.  
- Нижний пояс (dock-бар окон, time-строка): `H_dock ≈ 0.146 · H`.  
- Центральное рабочее поле (граф + панели): `H_center ≈ 0.708 · H`.

### 13.4. Пропорции окон и панелей

Стандартные aspect ratio:

- Горизонтальная панель (например, нижний лог): `ratio ≈ ϕ : 1` (ширина к высоте).  
- Вертикальная док-панель (Detail/Agent справа): `ratio ≈ 1 : ϕ`.  
- Плавающее окно: минимальные размеры кратны `U` и стремятся к ближайшему золотому прямоугольнику, т. е.  
  - ширина `W_pane ≈ 34U` (272 px), высота `H_pane ≈ 21U` (168 px) или их кратные.

При ресайзе окна включается «магнит» к гармоническим пропорциям: размеры округляются к ближайшим значениям, соответствующим `ϕ`-отношениям.

### 13.5. Ноды, глифы, кнопки

Пусть `R_s` — радиус маленькой ноды.

- Маленькая нода: `R_s = 3U` (24 px).  
- Средняя нода: `R_m ≈ ϕ · R_s ≈ 4.85U` (округляем до `5U = 40 px`).  
- Крупная нода (ядро кластера): `R_l ≈ ϕ · R_m ≈ 8U` (64 px).

Глиф внутри ноды:

- Диаметр глифа `D_glyph ≈ 0.618 · (2R_node)` (ϕ⁻¹ от диаметра ноды),  
- отступы от краёв ноды кратны `U` (например, 1U–1.5U).

Кнопки и иконки:

- Базовый размер иконки: `24 × 24 px` = `3U × 3U`.  
- Круглые кнопки: диаметр `= ϕ · 24 ≈ 38.8 px` → округление до `40 px` (5U).  
- Мини-кнопки (иконки управления окнами) — размер `2U` (16 px).

Минимальное расстояние между центрами нод:

- `D_min ≈ ϕ · (R_node1 + R_node2)` — чтобы визуально кластеры «дышали» и не слипались.

### 13.6. Типографическая шкала

Базовый размер шрифта `F0 = 14 px`.

Модульная шкала по ϕ:

- `F1 ≈ F0 · ϕ ≈ 22–23 px` (заголовки),  
- `F−1 ≈ F0 / ϕ ≈ 9 px` (микро-метки, использовать очень ограниченно).  

Практически:

- Body: 14–16 px.  
- Subheading: 18 px.  
- Heading: 22–24 px.  

Все значения выравниваются по U (кратно 2 px / 4 px), чтобы сохранить пиксельную чистоту.

### 13.7. Анимации и временные константы

Используем e и ϕ как ориентиры длительностей:

- Очень короткие (feedback hover): `T_short ≈ 0.18–0.22 s` (≈ 1/e).  
- Стандартные переходы (открытие окна, zoom фокуса): `T_mid ≈ 0.3–0.4 s`.  
- Редкие «ритуальные» анимации (смена режима, фаза Time Spiral): `T_long ≈ 0.55–0.65 s` (≈ ϕ · T_mid).

Периоды «дыхания» интерфейса:

- Пульс glow у ключевых глифов: `T_breath ≈ 4π–6π s` (12.5–18.8 с), чтобы ощущалось как очень медленное, почти незаметное дыхание.

### 13.8. Harmonic Lock / Expert Mode

Для продвинутого пользователя и для «чистой» эстетики вводится режим **Harmonic Lock**:

- При включенном режиме все операции изменения размеров/расположения элементов (панелей, окон, нод) снапятся к ближайшим гармоническим значениям (по сетке U и отношениям ϕ).  
- В UI это может отображаться маленьким индикатором (глиф ϕ возле док-бара).  
- В выключенном режиме допускается произвольный ресайз, но система всё равно старается предлагать гармоничные пресеты.

Эта гармоническая система превращает экран в управляемый «кристалл»; пользователь, который знает, как работают ϕ/π/e-параметры, может сознательно подстраивать HUD и граф под свои состояния и задачи, не нарушая общей цельности формы.
