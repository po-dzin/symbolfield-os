# SymbolField OS v0.5 — Unified Roadmap Canvas

Цель: собрать в один фрейм все уровни системы (данные → граф → NOW/Graph Mode → миграция Obsidian) и зафиксировать чёткую **v0.5 Core**. Всё, что дальше (звук/микшер, агент-уровень, 3D GraphMorph, публичное API, стриминг) идёт как надстройка над этим ядром.

---

## 0. Target: Что такое v0.5 Core

**v0.5 = точка, в которой:**

- Есть **единое ядро данных** (Supabase Core Store) с объектами, связями и файлами.
- Есть **миграция из Obsidian** (one-shot импорт vault → SF-граф).
- Есть **NOW-режим**: чтение/редактирование markdown-нот внутри SF.
- Есть **HUD v0.3x+** (state panel, temporal dock, windows) на тех же данных.
- Есть **Graph Mode 2D v0.3–0.4**: создание/редактирование нод и связей.
- HUD и Graph Mode смотрят в **один и тот же Object Graph**.

3D GraphMorph, звук, агенты, API, стриминг **не входят в 0.5**, но опираются на это ядро.

### 0.1. Канонические ограничения v0.5

Чтобы не утонуть в сложности и противоречиях, для v0.5 жёстко фиксируем:

- **XP-ось (ядро):** HP / EP / MP / SP (+ опциональный QP/NP как спец-слои). Все старые обозначения (CP/DP и т.п.) считаются устаревшими и в схемы/БД не попадают.
- **Временная шкала (Twindow v0.5):** только `NOW / DAY / WEEK / MONTH / YEAR`. MOON / SEASON / ARC и прочие циклы — как расширения после 0.5.
- **Режимы:**
  - `mode` (🕳 / 🌀 / 🔆) — один глобальный режим дня/сессии.
  - `hyperMode` в v0.5 фактически зафиксирован как `NORMAL` (HIBERNATE / PRISMATIC — концепты для последующих версий, без сложного UI).
- **FieldState v0.5:** минимальный срез поля, который агент и HUD могут читать как JSON-объект:
  - `mode`, `tone`, `xpSummary { hp, ep, mp, sp, qp? }`,
  - `timeWindow` (NOW/DAY/WEEK/MONTH/YEAR),
  - `graphStats { nodes, edges, activeClusterIds }`.
- **Активные слои v0.5 (MVP ядро):**
  - Core Store + ObjectGraph (минимальный набор сущностей и связей),
  - Obsidian one-shot импорт,
  - Day HUD (state\_bar + xp\_matrix + time\_spiral),
  - Graph Mode 2D (создание/редактирование базового графа),
  - один простой `/today`-флоу (быстрый вход: выбрать Mode/Tone, отметить 1–3 ритуала, сразу увидеть отражение в HUD),
  - Agent L0 (контекстный чат без прямых мутаций графа).

Всё остальное (полные HyperModes, расширенные временные шкалы, глубокие матрицы SSA/SEM7, AudioGraph, 3D/AR/VR, continuity-миф) — выносится в расширения после 0.5.

---

## 1. Уровни системы

### 1.1. Level A — Core Store & Migration

**Задача:** сделать устойчивое базовое хранилище и путь переноса мозга из Obsidian.

Состоит из:

1. **Supabase схемы (Core Store)**

   - `sf_files` — markdown-контент.
   - `sf_objects` — ноды графа (practice, concept, arc, project, ritual, etc.).
   - `sf_relations` — связи между объектами.

2. **One-shot Obsidian Importer**

   - Node/TS-скрипт, который:
     - читает все `.md` из vault,
     - парсит frontmatter, `[[links]]`, `#tags`,
     - создаёт `sf_files` (контент),
     - создаёт `sf_objects` (1 файл = 1 объект),
     - создаёт `sf_relations` по wiki-links.

3. (Опционально после 0.5) **Obsidian plugin → live-sync** (только если окажется реально нужен).

---

### 1.2. Level B — Object Graph Engine

**Задача:** обернуть Core Store в удобный слой запросов для HUD и Graph Mode.

Компоненты:

- **Тайпинг:**
  - `Space`, `XPLog`, `StateSnapshot`, `Session`, `Ritual`, `Glyph`, `SFObject`, `SFRelation` и т.п.
- **Запросы (API/Object Graph Integration)**:
  - `getXpSummary(spaceId)`
  - `getStateTimeline(spaceId, range)`
  - `getGraphEgo(nodeId, depth)`
  - `searchObjects(query, filters)`
- **GraphSharedState (frontend store):**
  - `activeSpaceId`
  - `activeObjectId`
  - `activeFileId`
  - `twindow` (NOW / DAY / WEEK / MONTH / YEAR)
  - фильтры (XP, SSA, типы нод и т.п.).

Это тот слой, который в HUD roadmap обозначен как **v0.5 → v0.555 — Object Graph Integration**, но здесь он расширен до полноценного ядра для всего UI.

---

### 1.3. Level C — Interfaces (HUD / NOW / Graph Mode 2D)

**Задача:** дать живой интерфейс к ядру.

1. **HUD (v0.301–0.37 + 0.6)**

   - Визуальная идентичность, палитра, сигил, доки.
   - State Panel (MODE / TONE / GLYPH) в кольцах.
   - Temporal Dock (NOW → DAY → WEEK → MONTH → YEAR).
   - Breath Pulse (мягкая пульсация ядра и доков).
   - Draggable windows + safe zones + glassmorphism.
   - HUD Dashboard читает данные через Object Graph Engine.

2. **NOW Mode (Markdown Editor)**

   - Открытие ноды/файла → загрузка `sf_files.content`.
   - Режимы: `view` (рендер md) / `edit` (редактор).
   - Правая панель: тип объекта, glyph, SSA-слой, связи.
   - Связь с GraphSharedState: `activeFileId` + `activeObjectId`.

3. **Graph Mode 2D (v0.4x)**

   - **v0.4 — Graph Mode v0.1:**
     - пустой canvas,
     - SOURCE → Core-нода,
     - single-click → properties,
     - double-click → вход в патч.
   - **v0.41–0.42 — Links + Components:**
     - порты нод + drag-connect линков,
     - shift+drag → новая нода + связь,
     - свойства нод: Glyph/Tone/XP/Temporal/Process/Ritual Components.
   - Graph Mode использует те же `sf_objects`/`sf_relations` и GraphSharedState.

---

### 1.4. Level D — Надстройки (после 0.5)

Эти уровни опираются на v0.5 Core, но не входят в него.

1. **Audio L0 + Mixer**

   - SFX для входа/выхода/кликов.
   - Ритуальная лестница (гонг → колокол → чаша → караталы → чеймсы → ультра-синус).
   - Мини-микшер в HUD: Master / Ambient / Ritual / SFX.

2. **GraphMorph 3D Viewer**

   - 3D макро-витрина поля (Three.js).
   - Только просмотр/анализ, без редактирования структуры.

3. **Agent Layer (FieldState Engine)**

   - **Agent L0** — базовый чат-помощник в вкладке Agent в HUD, знает контекст сессии и базовый профиль, но не трогает граф напрямую (только тексты/подсказки).
   - **Agent L1** — graph-aware FieldState-агент, который читает Object Graph + FieldState (mode/tone/XP/twindow/метрики графа), анализирует патчи и предлагает осмысленные действия/фокусы, но сам не мутирует структуру.
   - **Agent L2** — оркестратор, который через безопасный GraphEventBus/API может запускать экшены в системе (создание/рефакторинг нод, переключение режимов, запуск ритуалов, Stream Mode).

4. **External API**

   - Публичный REST/GraphQL слой поверх Object Graph Engine.
   - Интеграции с другими сервисами.\*
   - Публичный REST/GraphQL слой поверх Object Graph Engine.
   - Интеграции с другими сервисами.

5. **Stream Mode**

   - Режим, где GraphMorph 3D + аудио оптимизированы под стрим (OBS/RTMP/WebRTC).

---

## 2. Версионный план до v0.5

Ниже — линейный план, как пройти от нуля до чёткой v0.5 Core.

### v0.1 — Supabase Core Store

**Цель:** завести ядро данных.

- Создать таблицы: `sf_files`, `sf_objects`, `sf_relations`.
- Написать простые admin-скрипты/консольки для ручного создания тестовых объектов.
- Поднять базовый SDK на фронте (Supabase client).

### v0.2 — NOW Read + Simple Graph View

**Цель:** начать читать данные из ядра.

- NOW Mode v0.1:
  - выбрать объект из списка → показать markdown (`sf_files.content`) в режиме read-only.
- Graph View v0.1:
  - читать `sf_objects`/`sf_relations`,
  - рисовать простой 2D-граф (без редактирования).
- Связать клики на графе с NOW-view (activeObjectId → activeFileId).

### v0.25 — Obsidian One-shot Import

**Цель:** перенести текущий мозг.

- Node/TS-скрипт для импорта vault:
  - `.md` → `sf_files` + `sf_objects`,
  - `[[links]]` → `sf_relations`.
- Прогнать твой основной vault → получить первый живой SF-граф.

### v0.3 — HUD Shell (согласно HUD roadmap)

**Цель:** построить SF HUD поверх моков/Core Store.&#x20;

- v0.301–0.31: палитра, сигил, State Panel cleanup.
- v0.32: `activeTab` (HUD / Graph / Agent / Log / Settings).
- v0.33: Temporal Dock v0.1 (NOW → DAY → WEEK → MONTH → YEAR).
- v0.34: Agent L0 — базовый чат-помощник во вкладке Agent, использующий контекст текущего Space/Day/State, без прямых мутаций графа.
- v0.35: Breath Pulse v0.1.
- v0.36–0.37: Draggable Windows + Safe Zones + Glassmorphism.
- v0.38: `/today` flow v0.1 — быстрый пользовательский сценарий: выбрать Mode/Tone, отметить 1–3 ритуала, сразу увидеть отражение в `state_bar`, `xp_matrix` и `time_spiral`.

На этом этапе HUD ещё может работать на мок-данных или простых запросах из Supabase.

### v0.4 — Graph Mode 2D v0.1–0.3

**Цель:** дать реальный конструктор графа.

- Graph Mode v0.1: SOURCE → Core-нода, базовые ноды.
- Graph Mode v0.2: связи (drag-connect, shift+drag, удаление рёбер).
- Graph Mode v0.3: компоненты нод (Glyph/Tone/XP/Temporal/Process/Ritual).
- Graph Mode читает/пишет напрямую в `sf_objects` и `sf_relations` через Object Graph Engine.

### v0.5 — Core Integration (Object Graph + HUD + NOW + Migration)

**Цель:** собрать всё воедино.

- Object Graph Engine v1:
  - ввести типы Space/XPLog/StateSnapshot/Session/Ritual/Glyph,
  - реализовать базовые queries (`getXpSummary`, `getStateTimeline`, `getGraphEgo`).
- HUD переходит с моков на реальные данные из Object Graph Engine.
- NOW Mode получает полноценный read/write по `sf_files`.
- Graph Mode 2D окончательно пишет в Core Store и обновляет HUD через GraphSharedState.
- Obsidian-import становится стандартным шагом инициализации (bootstrap).

После v0.5 у тебя:

- единая база,
- единый граф,
- HUD и Graph Mode как два взгляда на одну реальность,
- возможность жить только в SF без необходимости открывать Obsidian.

---

## 3. После v0.5: расширение

После фиксации v0.5 Core можно разворачивать надстройки:

- **v0.6 — Audio L0 + Mixer**

  - SFX + ambient + лестница ритуальных звуков.
  - Мини-микшер в HUD.

- **v0.7 — GraphMorph 3D Viewer**

  - 3D макро-поле + базовые пресеты.

- **v0.8 — Agent L1 (FieldState-aware) + External API**

  - Graph-aware агент, читающий Object Graph + FieldState и предлагающий инсайты/действия внутри системы.
  - REST/GraphQL API для внешних сервисов поверх того же Object Graph Engine.

- **v0.9 — Agent L2 + Stream Mode**

  - Оркестратор, который через GraphEventBus/API может безопасно выполнять действия в системе (мутации графа, переключение модов, запуск ритуалов и Stream Mode).
  - Оптимизация 3D/аудио для стримов и трансляций.

Эти стадии уже опираются на устойчивый фундамент, собранный к v0.5.

---

## 4. DLC / Extended Modules (после v0.9)

Этот блок фиксирует всё, что сознательно вынесено за пределы v0.5–0.9, чтобы не висело “фантомной обязанностью”, но оставалось в официальном бэклоге эволюции системы.

1. **Полные HyperModes и Prismatic-патчи**

   - Глобальные `hyperMode = PRISMATIC / HIBERNATE` с UI, влияющим на звук, графику и логику.
   - Prismatic / Void-режимы для core-нод (reactable-патчи), с отдельными визуальными и аудиосценами.

2. **AudioGraph / zEngine**

   - Звук как отдельный подграф: источники, эффекты, сцены, маппинг на FieldState и граф.
   - Генеративный саундтрек, который реагирует на XP, Twindow, активные патчи.

3. **SSA5 / SEM7 / 6×4 HUD — глубокая интеграция**

   - Полное отображение слоёв SSA5 в UI (сенсорный / экспрессивный / символический / нарративный / мета-слой).
   - SEM7 как видимые стадии процессов/арок (Seed → Release) в HUD и графе.
   - HUD 6×4 как реально настраиваемая матрица поведения HUD, а не только чек-лист для дизайна.

4. **Cosmogenesis / SF Continuum Onboarding**

   - Сценарий “теофании”: SPACE×TIME×STATE-интро, Seed → Core → HUD в виде ритуальной вступительной последовательности.
   - Варианты онбординга для разных ролей (Seeker, Creator, Architect).

5. **Harmonic Layout Engine**

   - Жёсткая математика пропорций (ϕ, π, e) для сетки, нод, окон, дистанций.
   - Возможность включать/выключать “Harmonic Lock”, чтобы HUD/GraphMorph подстраивались под математически гармоничный режим.

6. **Obsidian Live Sync и другие knowledge-хабы**

   - Obsidian-плагин для one-way или two-way синхронизации через API (в реальном времени).
   - Коннекторы к Notion / Anytype / Logseq / Tana как внешним источникам объектов.

7. **GraphMorph 3D Edit Mode + AR/VR**

   - Не только 3D viewer, но и редактирование графа в 2.5D/3D (Constellation View, сферические/спиральные раскладки).
   - AR/VR-клиенты, где SF HUD и граф существуют как надстройка над физическим пространством.

8. **Stream Mode 2.0**

   - Глубокая интеграция с OBS / RTMP / WebRTC.
   - Presets для лайв-перформансов: сцены, граф-анимации, аудио-пресеты, которые включаются одним кликом.

9. **Agent L3 / Swarm**

   - Мультиагентная конфигурация: отдельные агенты под XP, структуру, звук, визуал, интеграции.
   - Orchestrator, который управляет роутингом запросов между агентами и пользователем.

10. **Интеграции с внешними креативными сервисами**

    - Прямые коннекторы к Suno / визуальным моделям / видео-движкам и т.п. для генерации артефактов на основе FieldState и локальных патчей.
    - Экспорт/импорт стилей, промптов и сетапов как объектов графа.

Все эти модули проектируются так, чтобы **не ломать v0.5–0.9 ядро**, а навешиваться на него как дополнительные кольца спирали — через тот же ObjectGraph, FieldState и GraphEventBus.

