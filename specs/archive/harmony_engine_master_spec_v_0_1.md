# HarmonyEngine — Unified SymbolField Harmonic Specification v0.1

**Единый свод:** Meta‑Harmonic Matrix + Unified SF Harmonic Core + Ultra‑Harmonic Mode.

Цель: создать математически целостный, эстетически согласованный и системно предсказуемый движок гармонизации всех визуальных, поведенческих и смысловых слоёв SymbolField OS.

---

# 1. Состояние системы (S)

**S(t) = { M, T, G, X, τ, 𝔾 }**

Где:

- **M** — mode (deep / flow / luma)
- **T** — tone (эмо-акцент, цвет)
- **G** — glyph-конфигурация
- **X = (HP, EP, MP, SP, QP)** — опыт
- **τ** — time-scale (day/week/month/year…)
- **𝔾** — граф (узлы, связи, веса)

Рендер-интерфейс:

> **V(t) = 𝓡(S(t); F, L, A)**

Где триада:

- **F = Form (○)** — структура
- **L = Flow (∣)** — динамика
- **A = Attention (•)** — фокус

---

# 2. Цветовая Мета-Формула

Все слои UI подчиняются системе «Mode → Tone → XP → Glow».

## 2.1 Mode (фон)

deep: Lₘ=6,  Sₘ≈8%

flow: Lₘ=12, Sₘ≈8%

luma: Lₘ=86, Sₘ≈35%

## 2.2 Tone (эмо-акцент)

> **Lₜ = clamp(Lₘ + 50, 60, 72)**
> **Sₜ = Sₘ + ΔSₜ (30–60%)**

## 2.3 XP-цвета

> **Lᵢ = Lₜ − 3**
> **Sᵢ = 0.9·Sₜ**

## 2.4 Glow

> **Lᵢ^glow = Lᵢ + 12**
> **Sᵢ^glow = Sᵢ + 15%**

---

# 3. Геометрия и Пульсации

XP управляет формой, расстоянием, яркостью.

## 3.1 Радиус орбиты XP-ноды

> **rᵢ = r₀ + α·√xᵢ**

## 3.2 Сложность паттерна

> **kᵢ = kₘᵢₙ + β·xᵢ**

## 3.3 Пульсации

> **radiusᵢ(t) = rᵢ · [1 + ampᵢ · sin(2π·f·t + φᵢ)]**

Где:

- **ampᵢ = a₀ + μ·xᵢ**
- **f** — частота «дыхания» режима

---

# 4. Mode-поведение (Flow Engine)

Весовые коэффициенты:

- deep: **F=0.7, L=0.2, A=0.3**
- flow: **F=0.5, L=0.6, A=0.5**
- luma: **F=0.4, L=0.7, A=0.6**

Переходы:

**Частота дыхания:**

> **f = f₀·(1 + σ·L)**

**Плавность:**

> **τₑ = τ₀/(1 + L)**

**Контраст:**

> **contrast = 1 + ρ·A**

---

# 5. Glyph Engine

Глиф = набор параметров: **w (толщина), c (кривизна), b (разрывы), φ (поворот)**.

## 5.1 Mode → Glyph

- deep → w↑, b↑
- flow → c↑, w↓
- luma → φ(t), b↓

## 5.2 Tone → Glyph

> **w = w₀·(1 + κ·A)**

## 5.3 XP → Glyph

> **φᵢ(t) = φ₀ + ω·xᵢ·t**

---

# 6. Time Spiral

XP агрегируется во временные кольца.

Плотность XP:

> **Dᵢ(τ) = Σ XPᵢ(τ) / N(τ)**

Яркость орбит:

> **Lᵗ = L₀ + γ·D\_total**

Толщина орбит стихий:

> **thᵢ = θ₀ + λ·Dᵢ**

---

# 7. Graph Harmonics

Нода:

> **sizeᵢ = s₀ + η·log(1 + dᵢ)**

Свежесть:

> **Lᵢ = L\_min + κ·(1 − age(tᵢ))**

Связи:

> **th\_edge = θ₀ + λ·w**

Комбинация XP и связности:

> **weightᵢ = max( w\_links·log(1+dᵢ), w\_xp·√xp\_total )**

---

# 8. Ultra-Harmony Mode (Ультра-режим)

**Идея:** всё влияет на всё.

Матрица влияний H[a,b] ∈ [0,1]:

| from \ to | mode | tone | glyph | xp  | time | graph |
| --------- | ---- | ---- | ----- | --- | ---- | ----- |
| mode      | 1    | 0.8  | 0.6   | 0.2 | 0.3  | 0.2   |
| tone      | 0.2  | 1    | 0.5   | 0.4 | 0.3  | 0.1   |
| glyph     | 0.1  | 0.2  | 1     | 0.2 | 0.1  | 0.4   |
| xp        | 0.1  | 0.3  | 0.3   | 1   | 0.6  | 0.5   |
| time      | 0.1  | 0.2  | 0.2   | 0.5 | 1    | 0.4   |
| graph     | 0.1  | 0.2  | 0.3   | 0.4 | 0.4  | 1     |

При включении:

> **Δstate = H · baseInfluences**

Система становится реактивной, сверхчувствительной, живой.

---

# 9. Базовые размеры и пропорции (ϕ · π · e)

## 9.1. Константы гармонии

ϕ ≈ 1.618 — золотая пропорция формы\
π ≈ 3.14159 — циклы, орбиты, дыхание\
e ≈ 2.718 — темп изменений, экспоненциальные затухания

**U = 8 px** — базовый модуль сетки.

## 9.2. Распределение пространства HUD и Graph

- **Центральный граф‑канвас:** W\_graph ≈ 0.618W
- **Навигационный левый док:** W\_nav ≈ 0.146W
- **Правый док-панель:** W\_dock ≈ 0.236W

Вертикально:

- Header: 0.146H
- Bottom dock (Time Spiral): 0.146H
- Рабочая зона: 0.708H

## 9.3. Панели и окна

- Горизонтальная панель: ratio ≈ ϕ:1
- Вертикальная панель: ratio ≈ 1:ϕ
- Плавающее окно: 34U×21U (272×168px) или кратные.

## 9.4. Ноды, глифы, кнопки

- Маленькая нода: **R\_s = 3U = 24px**
- Средняя: **R\_m = 5U = 40px**
- Крупная: **R\_l = 8U = 64px**
- Глиф внутри ноды: D\_glyph ≈ 0.618·диаметра
- Кнопки круглые: 5U (40px)
- Мини-кнопки: 2U (16px)
- Минимальная дистанция нод: D\_min ≈ ϕ · (R1 + R2)

## 9.5. Типографика

- Base: 14–16px
- Subheading: 18px
- Heading: 22–24px
- Модульная шкала по ϕ.

## 9.6. Анимации

- Hover: 0.18–0.22s (≈1/e)
- Переходы: 0.3–0.4s
- Ритуальные переходы: 0.55–0.65s (≈ϕ·T\_mid)
- Дыхание glow: 12–18s (≈4π–6π)

---

# 10. Режимы графа — техническая спека

## 10.1. Режим A — плоский граф (Flat Node View)

**Назначение:** базовая навигация, чтение, связи, работа с данными.

### Модель данных

**Node { id, type, title, glyph, xp\_state, tags, time\_tags, position2D, cluster\_id? }**\
**Edge { id, source\_id, target\_id, relation\_type, weight, directed }**\
**Cluster { id, label, color, nodes[] }**

### Пространство

2D‑холст бесконечный: координаты в диапазоне [-1000, 1000] или больше.

### Лэйауты

- force‑directed / spring — по умолчанию
- radial для корневых структур

### Zoom‑LOD

- far → точки кластеров
- mid → ноды + тайтлы
- near → ноды + XP + glyph‑детали

### Интеракции

- ЛКМ → открыть свойства ноды
- Alt+ЛКМ → вызвать Agent‑панель
- Shift+drag → множественный выбор
- Scroll/pinch → zoom



### Производительность

До 300–500 нод — комфортно.\
Выше → авто‑кластеризация.

---

## 10.2. Режим B — Constellation / Bio‑Cloud (2.5D → переход к 3D)

**Назначение:** созерцание, ритуалы, презентации.

### Модель данных (дополнительно)

- depth для нод (−1, 0, +1)
- sphere\_radius для кластеров
- orbit‑edges

### Рендеринг 2.5D

- Z‑глубина → масштаб и прозрачность
- Кластеры → полупрозрачные сферы
- Параллакс движений

### Интеракции

- Hover по кластеру → “выныривает”
- Клик → zoom внутрь
- Клик по ноде → Detail/Agent
- Hotkey → Flat ↔ Constellation (анимация интерполяции позиций)

### LOD

- far → только сферы
- mid → ноды + связи
- near → орбиты, микро‑спайки

### Будущее

Этот режим → прототип полноценного 3D GraphMorph.

---

# 11. Harmonic Lock (режим ϕ‑кристалла)

При включении:

- все размеры и позиции снапятся к сетке U и отношениям ϕ
- плавающие окна стремятся к золотым прямоугольникам
- ноды и кластеры занимают гармоничные расстояния

Индикатор: маленький глиф ϕ рядом с док‑баром.

---

# 12. Harmonic Engine Integration (обновлённое ТЗ)

1. HarmonyEngine получает **S(t)** и возвращает полный набор: цвета, размеры, glow, геометрию, анимации, параметры графа.
2. Включает 5 подмодулей:
   - ColorEngine
   - GeoEngine
   - GlyphEngine
   - TemporalEngine
   - GraphHarmonics
3. Ultra‑Harmony и Harmonic Lock — переключатели в Settings.
4. Все UI‑элементы подписываются на HarmonyEngine.
5. Все константы, таблицы и формулы вынесены в `harmonics.ts`.

---

Готово: единый, полный, согласованный HarmonyEngine Core v0.1.

