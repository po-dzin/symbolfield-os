# 🚀 SymbolField OS — Agent-Based Development Strategy

> Контекст: Google Antigravity, macOS M1 (8GB), React frontend, Python core

---

## Архитектура цикла

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  Architect  │───▶│  Developer  │───▶│ QA Guardian │───▶│ Human (You) │
│   (Plan)    │    │   (Code)    │    │(Test+Review)│    │  (Approve)  │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
       │                                      │                   │
       └──────────────── Memory Store ───────┴───────────────────┘
                      (Chroma / Supabase)
```

---

## Этапы выполнения

### Этап 1: Установка среды
- Выполняется **один раз** на машине
- Скрипт: `setup.sh`

### Этап 2: Структура проекта
```
agents/          # Определения агентов (роли, backstory)
tools/           # Инструменты для агентов (API, файлы)
memory/          # Persistent memory (ChromaDB / Supabase)
spec.md          # Текущая задача (human input)
tasks.json       # Atomic task units
.env             # API ключи
```

### Этап 3: Агенты (CrewAI)

| Агент | Роль | Цель |
|-------|------|------|
| **Symbol Architect** | Проектировщик | Символически непротиворечивая архитектура |
| **MetaCoder** | Разработчик | Абстрактный код по спецификации |
| **QA Guardian** | Тестировщик + Ревьюер | Написание тестов (TDD), запуск `vitest`, анализ отчетов + code-review |

### Этап 4: Процесс (Hybrid QA Strategy)
1. **Architect** → Проектирует модуль.
2. **QA Guardian** → Создает `test_spec.md` и пишет падающие тесты (*.test.jsx*).
3. **Developer** → Пишет реализацию, пока тесты не пройдут.
4. **QA Guardian** → Запускает `npm run test` для верификации и делает финальный аудит.
5. **Human (You)** → Финальное решение.

### Этап 5: Human-in-the-Loop
- Редактируешь `spec.md` с описанием задачи
- Запускаешь `python main.py`
- Просматриваешь результат

### Этап 6: Итерации
- Если баги → повторяем задачу Developer/Tester
- Если ОК → Reviewer вносит в changelog

### Этап 7: Фиксация результатов
- Обновляем README.md
- Сохраняем spec.md
- Git commit

---

## Артефакты цикла

| Файл | Назначение |
|------|------------|
| `spec.md` | Задача (input) |
| `symbol_map.py` | Код (output) |
| `test_symbol_map.py` | Тесты |
| `review.md` | Вывод ревьюера |
| `changelog.md` | История изменений |

---

## Расширение

После 1–2 итераций можно добавить:
- **DocsAgent** — автодокументация
- **DesignAgent** — UI/UX спецификации
- **DeployAgent** — CI/CD автоматизация
